let baseUrl = $('#txt_baseUrl').val();

const EMPLOYEES = (function(){

	let thisEmployee = {};

	var Toast = Swal.mixin({
    toast: true,
    position: 'top',
    showConfirmButton: false,
    timer: 3000
  });

  

  return thisEmployee;

})();